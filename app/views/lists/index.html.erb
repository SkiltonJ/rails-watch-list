<div class="background">
  <div class="banner" style="background-image: url(<%=image_path "movies_banner.jpg" %>)">
    <div class="container">
      <h1>Your lists</h1>
    </div>
  </div>
  <div class="container">
    <div class="list">
     <div class="container text-center">
      <div class="row">
        <% @lists.each do |list| %>
          <div class="col list-card">
            <%= link_to list_path(list) do %>
              <%= list.name %>
            <% end %>
          </div>
        <% end %>
      </div>
    </div>
    <div class="link">
      <%= link_to '✚ Create new movie list', new_list_path %>
    </div>
  </div>
</div>
